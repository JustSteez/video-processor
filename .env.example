# Redis connection — matches the service name in docker-compose.yml
REDIS_URL=redis://redis:6379

# For local dev outside Docker, use:
# REDIS_URL=redis://localhost:6379

# Random secret for the cleanup endpoint — generate with: openssl rand -hex 32
CLEANUP_SECRET=replace-with-a-random-secret
